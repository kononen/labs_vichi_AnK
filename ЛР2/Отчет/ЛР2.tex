 \documentclass[12pt,a4paper]{article}

\usepackage[cp1251]{inputenc}
\usepackage[T2A]{fontenc}
\usepackage[russian]{babel}
\usepackage{hyperref}
\usepackage[footnotes,oglav,spisok,boldsect,eqwhole,remarks,kursrab, hyperprint]{project}

\newcommand{\cond}{\mathop{\mathrm{cond}}}

\begin{document}
\cover{Методы вычислений}{Исследования обусловленности задачи решения СЛАУ.}{студенты группы ФН2-51}{Бондарчук~Виктория и\: Мукова~Рагнеда}{\centerline{\Large{Вариант 2; 7}}}{}{}{}{2016}
%\tableofcontents
\newpage

\section{Исходные данные}

Рассмотрим систему линейных алгебраический уравнений вида:
\begin{gather}
A x=b,
\label{sys}
\end{gather}
где $A$ --- матрица системы размера $n \times n$, $\det A \ne 0$, $b$ --- вектор правой части.

\section{Описание используемы алгоритмов}

Будем считать, что система (\ref{sys}) имеет единственное решение.

В лабораторной работе заданная система решается методом QR-разложения, реализованным в лабораторной работе №1.
\subsection{Число обусловленности}

Пусть $\Delta b$ --- погрешность, с которой задана правая часть системы (\ref{sys}). Тогда в результате решения системы
\begin{gather}
A (x+\Delta x)=b+ \Delta b
\label{sys2}
\end{gather}
решение исходной задачи (\ref{sys}) будет найдено с погрешностью $\Delta x$, удовлетворяющей условию:
$$
A \Delta x=\Delta b \quad \text{или} \quad \Delta x=A^{-1} \Delta b.
$$  

Учитывая неравенство: 
$$
\|b\| \le \|A\| \cdot \|x\|,
$$
получаем:
\begin{gather}
\frac{\|\Delta x\|}{\|x\|} \le \|A^{-1}\| \cdot \|A\| \cdot \frac{\|\Delta b\|}{\|b\|}.
\label{obusl}
\end{gather}

Величину $\cond A=\|A^{-1}\| \cdot \|A\|$ называют числом обусловленности матрицы $A$.  

\subsection{Оценка числа обусловленности}

Из соотношения (\ref{obusl}) следует неравенство:
$$
\cond A=\frac{\delta x}{\delta b},
$$
где
$$
\delta x=\frac{\|\Delta x\|}{\|x\|} \qquad \delta b=\frac{\|\Delta b\|}{\|b\|},
$$
--- относительные погрешности решения и правой части соответственно.

Для получения нижней оценки числа обусловленности, система (\ref{sys2}) решается нескольно раз с разными $\Delta b$ и выбирается наибольшая величина отношения относительных погрешностей решения и правой части. 

\newpage
\section{Результаты расчетов}


\newpage
\section{Анализ результатов}

Число обусловленности характерезует чувствительность решения системы к малым погрешностям входных данных. Если система хорошо обусловленна, то при малых возмущениях правой части, решение изменится не сильно. Если же система плохо обусловлена, то решение изменится сильно. 

Число обусловленности, в следствии своих свойств, всегда удовлетворяет неравенству: $\cond A \ge 1$, но можно посчитать более точную оценку, используя абсолютные и относительные погрешности решения и правой части системы. Оценка числа обусловленности бывает близка к самому числу обусловленности, но бывают случаи, когда они значительно отличаются.

Число обусловленности отличается для разных норм, но порядок всегда остается один и тот же, так как нормы эквивалентны.

 


\section{Ответы на контрольные вопросы}
\begin{enumerate}
\item \textit{Что такое число обусловленности? Что оно характерезует?}\\
\\
Величину $\cond A=\|A^{-1}\| \cdot \|A\|$ называют числом обусловленности матрицы $A$. Число обусловленности характерезует чувствительность решения системы $A x=b$ к малым погрешностям входных данных. 
\\
 \item \textit{Как упрощается оценка числа обусловленности, если матрица является:
\begin{enumerate}
\item диагональной;
\item симметричной;
\item ортогональной;
\item положительно определенной;
\item треугольной?
\end{enumerate}}
\begin{enumerate}
\item Пусть матрица коэффициентов СЛАУ имеет вид:
$$
A=
\begin{pmatrix}
a_{11} & 0 & 0 & \ldots & 0\\
0 & a_{22} & 0 & \ldots & 0\\
\vdots & \vdots & \vdots & \ddots & \vdots\\
0 & 0& 0 & \ldots & a_{nn}
\end{pmatrix}
$$
Тогда коэффициенты $a_{ii}, \, i=\overline{1,n}$ --- собственные числа матрицы $A$. 
Так как диагональная матрица --- частный случай симметричной, то оценка для числа обусловленности матрицы $A$ имеет вид:
$$
\cond A=\frac{|\lambda_{max}|}{|\lambda_{min}|}.
$$
Учитывая, что собственные числа матрицы $A$ --- это ее элементы, получаем:
$$
\cond A=\frac{|\lambda_{max}|}{|\lambda_{min}|}=\frac{\max\limits_i {|a_{ii}|}}{\min\limits_i {|a_{ii}|}}
$$
\\
\item Пусть матрица $A$ --- симметрична, то есть опрератор $A$ --- самомопряженный.\\
Рассмотрим пространство $H$ с евклидовой нормой $\|x\|^2=(x,x)$. Тогда оператор $A$ имеет $n$ собственных векторов, образующих ортонормированный базис, и $\forall x \in H$ можно записать:
$$
x=\sum\limits^n_{i=1}{c_i x_i},
$$
причем $\|x\|^2=\sum\limits^n_{i=1}{c_i^2}$.\\
Тогда получаем:
\begin{gather*}
Ax=\sum\limits_{i=1}^n{c_i \lambda_i x_i},\quad \|Ax\|^2=\sum\limits_{i=1}^n{c_i^2 \lambda_i^2}.
\end{gather*}
Отсюда, $\|A\|=|\lambda_{max}|$. Таким же образом можно получить $\|A^{-1}\|=|\lambda_{min}|^{-1}$. \\
Следовательно, $\cond A=\dfrac{|\lambda_{max}|}{|\lambda_{min}|}$, причем все собственные значения симметричной матрицы $\lambda \in \mathbb{R}$.
\\
\item Пусть матрица коэффициентов СЛАУ $A$ --- ортогональная, то есть $A^{-1}=A^T$. Модуль каждого собственного значения ортогональной матрицы равен единицы, тогда:
$$
\cond A \ge 1.
$$

\item Пусть матрица коэффициентов СЛАУ $A$ --- положительно определена, то есть $\forall x\ne 0$ квадратичная форма $(Ax,x)>0$. Все собственные значения положительно определенной матрицы --- положетельны, тогда:
$$
\cond A\ge\frac{\lambda_{max}}{\lambda_{min}}.
$$

По теореме Гершгорина: Все собственные значения матрицы $A$ лежат в объединении кругов $S_1, S_2, \ldots, S_n$, где $S_i=\{z \in \mathbb{C}: |a_{ii}-\lambda| \le \sum\limits_{j \ne i}a_{ij}\}$. Если объединение $i$ изолированных друг от друга кругов образует связное множество, то в этом множестве находится ровно $i$ собственных значений. Так как для положительно определенной матрицы $\lambda>0, \, \lambda \in \mathbb{R}$,то достаточно рассмотреть лишь отрезки с центрами на положительноц части действительной полуоси. Обозначив точную верхнюю грань связного множества $\lambda_{sup}$, а точную нижнюю грань --- $\lambda_{inf}$, получим следующую оценку обусловленности матрицы $A$:
$$
\cond A \ge \frac{\lambda_{sup}}{\lambda_{inf}}.
$$
\item Пусть матрица коэффициентов СЛАУ имеет вид:
$$
A=
\begin{pmatrix}
a_{11} & a_{12} & a_{13} & \ldots & a_{1n}\\
0 & a_{22} & a_{23} & \ldots & a_{2n}\\
\vdots & \vdots & \vdots & \ddots & \vdots\\
0 & 0 & 0 & \ldots & a_{nn}
\end{pmatrix}.
$$
Собственные значения треугольной матрицы стоят на главной диагонали. Тогда:
$$
\cond A\ge \frac{\max\limits_i{|a_{ii}|}}{\min\limits_i{|a_{ii}|}}.
$$
\end{enumerate}
\item \textit{Имеется ли связь между обусловленностью и величиной определителя матрицы?}\\
\\
Обобщенным понятием вырожденной системы является плохо обусловленная система. Системы <<близкие>> к вырожденным скорее всего будут плохо обусловленными.\\
Исключение: СЛАУ с матрицей коэффициентов вида $A= \alpha E$, где $\alpha \ll 1$, число обусловленности таких матриц $\cond A=1$.
\\
 \item \textit{Как влияет выбор нормы матрицы на оценку числа обусловленности?}\\
\\
Числа обусловленности одной и той же матрицы при выборе разных норм различны, но всегда имеют одинаковый порядок, кроме того всегда выполняется $\cond A\ge 1$.
\\
 \item \textit{Применимо ли понятие числа обусловленности к вырожденным матрицам?}\\
\\
Пусть матрица $A$ --- вырожденная. Если матрица вырожденная, то у нее есть собственное значение $\lambda=0$. Тогда $|\lambda_{min}|=0$.
Следовательно, 
$$
\cond A\ge \frac{|\lambda_{max}|}{|\lambda_{min}|},
$$
но, так как $|\lambda_{min}|=0$, то можно записать, что для вырожденной матрицы $\cond A=\infty$.
\\
\item \textit{Объясните, почему, говоря о векторах, норму $\|\cdot\|_1$ часто называют октаэдрической, норму $\|\cdot\|_2$ --- шаровой, а норму $\|\cdot\|_{\infty}$ --- кубической.}\\
\\
Норма вектора $\|\cdot\|_1$ называется октаэдрической, потому что если откладывать все векторы от одной точки, то концы векторов, удовлетворяющих равенству $\|x\|_1 = a$ заполнят октаэдр с диагональю $a$ в трехмерном пространстве.\\
Норма вектора $\|\cdot\|_2$ называется шаровой, потому что если откладывать все векторы от одной точки, то концы векторов, удовлетворяющих равенству $\|x\|_2 = a$ заполнят шар с радиусом $a$ в трехмерном пространстве. \\
Норма вектора $\|\cdot\|_{\infty}$ называется кубической, потому что если откладывать все векторы от одной точки, то концы векторов, удовлетворяющих равенству $\|x\|_{\infty} = a$ заполнят куб со стороной $a$ в трехмерном пространстве. 
\\ 
\end{enumerate}
\end{document} 